<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">BarBestellSystem2</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link px-2" href="TableMap">
                <span class="mdi mdi-table-chair"></span>
                <span class="mx-2">Tische</span>
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link px-2" href="kitchen/ordersoverview">

                <span class="mdi mdi-pot-steam"></span>
                <span class="mx-2">Küche</span>
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link px-2" href="AdminPage/Tables" Match="NavLinkMatch.Prefix">
                    <span class="mdi mdi-cog"></span>
                <span class="mx-2">Einstellung</span>
            </NavLink>
        </div>

    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}